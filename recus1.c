#include<stdio.h>
#include<stdlib.h>

int multiplicacao(int);

int main(int argc, char *argv[])
{
	unsigned int a;
	
	return EXIT_SUCCESS;
}

int multiplicacao(b)
{
	if(b == 1)
		return 1;
	else
		return multiplicacao(b);
}
